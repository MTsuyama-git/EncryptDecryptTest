#compdef decrypt

_decrypt() {
    _arguments '1: :->input'
    local -a inputs
    inputs=(hoge foo bar)
    case "$state" in
	input)
	    _values $state $(find . -type f -name \*.aes)
	    ;;
    esac
}

compdef _decrypt decrypt
